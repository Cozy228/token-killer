/**
 * Flagship B6 — symbol biography (M2-ACCEPTANCE.md "Flagship"). Closes M2, owned
 * by slice 2d, requires 2b + 2c merged. Wired as `test.todo` in 2a.
 */
import { describe, test } from "vitest";

describe("acceptance: 2d flagship biography", () => {
  test.todo(
    "B6-biography: context(ref:'sym:<real M1 symbol>') returns in ONE call — definition (N⇥ numbered, G-8 clean), symbol-level history (B2), anchored memory (a remember() note, drift-honest per B3), callers preview with drill handles (⚠ record the exact symbol + anchored note)",
  );
});
