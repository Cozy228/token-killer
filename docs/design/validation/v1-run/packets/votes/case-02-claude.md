---
case: 2
voter: claude
date: 2026-07-12
---

# Case 2 vote — token-killer#89 "fix: preserve JVM recovery evidence" (claude)

Pinned worktree: `git worktree add --detach /private/tmp/v0-vote-02-claude-x7q3 59fc1ab217dd4c37ccc1ccdac5ff6487b9cb25ec`. Pre-PR state read at the merge commit's sole parent `212659f6` (squash merge, verified via `git log --format='%h %p'`). GitHub records checked against cutoff 2026-07-09T06:09:56Z (PR merged_at equals cutoff; all API records cited below carry created_at at or before it).

## Q1 — routing + behavior changes beyond the advertised fixes

**Score: partial (5/6)**

I independently re-derived the routing and the delta set. Routing: maven `match()` is `program === "mvn" || program.endsWith("mvn.cmd")` and gradle `match()` is `gradle | ./gradlew | gradlew | *gradlew.bat`, byte-identical pre/post PR (worktree `src/handlers/java/maven.ts:224-226`, `gradle.ts:262-269` vs `git show 212659f6:src/handlers/java/{maven,gradle}.ts` lines 56-58 / 44-50), and `ParsedCommand.program` is the raw first token, never basename-normalized (`src/parse.ts:52`), so `mvnw`/`./mvnw`/`mvnw.cmd` never reach the handler — the operator's "routing untouched" and pre-existing mvnw gap both check out. Verified deltas: old `BUILD_VERBS = ^(compile|test|package|install|deploy|verify|validate|clean|site|build)$` (212659f6 maven.ts:8) vs new `MAVEN_PHASES` (maven.ts:17-26) confirms exactly `clean/site/validate/build` drop out and `test-compile`/`integration-test` join; verbose flags newly force passthrough (maven.ts:177, and gradle previously compressed every invocation — 212659f6 gradle.ts:53-56 had no task detection — so unknown-task/verbose gradle passthrough is real). Sub-claims: (1) routing spellings + unchanged — correct; (2) mvnw gap pre-existing — correct; (3) four verbs exit compression — correct; (4) verbose/unknown newly passthrough — correct (minor overstatement: maven probe flags like `--version` were already passthrough under the old `isBuildVerb` test, so "probe" is not a delta); (5) two new phases join — correct; (6) in-scope plugin goals newly join compression — MISSED: `checkstyle/pmd/spotbugs/jacoco/spring-boot:*` previously passed through (colon-goals never matched the anchored `BUILD_VERBS` regex) and now map to phase `verify` and compress when a build footer is present (maven.ts:66-68). That is a passthrough→compressed delta beyond the four fixes, squarely what the question's "(or vice versa)" asked for, and it also falsifies the answer's blanket "they are direction-of-safety (passthrough = no information loss)" framing for that shape. Because the answer explicitly flags that behavior changes beyond the advertised fixes exist and gets five of six material sub-claims right, this is partial rather than false-reassurance: 5/6.

## Q2 — where is the original filtering + its rationale

**Score: correct**

Independently confirmed on admissible sources. `gh api repos/czync/token-killer/pulls/89/commits` (all committed 2026-07-08/09, pre-cutoff) shows exactly three commits: 0bed33a8 "docs: plan JVM RTK migration", 3dcdac52 "feat: expand JVM command output filtering", 1654785d "fix: preserve JVM recovery evidence" — so the offending filtering is the PR's own first feature commit and the repair is its second, visible in sequence to a commit-by-commit reviewer. `gh api .../commits/3dcdac52` shows it rewrote maven.ts (+184/-32) and gradle.ts (+235/-19); the file content at ref 3dcdac52 has the regressed forms (quiet gate without exit-code check at its line 182, `surefire-reports` only at line 158, no resume-command pattern), and the 1654785d patch adds `exitCode !== 0 &&`, the `(?:-rf|--resume-from)` line, the `(?:surefire|failsafe)-reports` regex, and the gradle `check` = test+analysis split. Main's ancestry (`git log 59fc1ab2 -- src/handlers/java/`) shows no other JVM-filter change between 06-10 and this merge, so "added on the 0.3.3 branch" can only mean this PR's branch (which carries the 0.3.3 bump). The rationale trail is as stated: the footer-derived-heading comment exists in the pre-PR code (212659f6 maven.ts:26), the 06-02→06-10 lineage is 43f37c3b / fc68a445 "align handlers with lossless RTK output" / a0bf8705 "output fidelity across …", no ADR among 0001-0015 (`git ls-tree 59fc1ab2 docs/adr/`) covers JVM filtering specifically, and the PR ships `docs/jvm-ecosystem-rtk-migration-plan.md` (569 lines in the merge stat).

## Q3 — verifiable test/CI evidence at cutoff

**Score: correct**

Every claim re-verified. The four fix claims map to named tests present in the merge tree: quiet-mode mislabel → `tests/unit/handlers/rtkMavenBehavior.test.ts:174` ("quiet success without an English footer stays raw instead of being labeled failed"); Failsafe report paths → same file :182 ("failsafe failure keeps report path for integration-test recovery"); reactor resume → :202 ("reactor failure keeps resume command for human recovery"); gradle `check` as test+analysis → `tests/unit/handlers/rtkGradleBehavior.test.ts:184` ("check task keeps test failure evidence as well as build status"). `gh api 'repos/czync/token-killer/actions/runs?head_sha=1654785d…'` returns one CI run: completed/success, created 2026-07-09T06:05:42Z, updated 06:09:52Z — green 4 seconds before the 06:09:56Z merge/cutoff, exactly as the operator stated. The qualifier on the body's broader numbers is also accurate and correctly scoped: the CI workflow (`.github/workflows/ci.yml`) runs typecheck, `pnpm test:product`, `pnpm test:install`, `validate-docs.sh`, and `tests/smoke/smoke.sh`, so those figures are corroborated to that extent, while the "54 JVM vitest" targeted-suite count remains an author claim. No confident overclaim found.

## Q4 — the output-dropping contract

**Score: correct**

The identified contract and its location are right. `docs/adr/0001-evidence-class-capping-and-recovery.md` (status: accepted) is the recorded contract: its Decision §1 explicitly separates *noise-removal* ("always allowed") from *evidence-capping* (governed, declared, "No `+N more` anywhere; reduction follows a two-step over-budget ladder") — which directly supports the operator's "noise-line stripping remains undeclared by design; ADR 0001 governs evidence capping". `OmissionDeclaration` is defined at `src/types.ts:32` and both rewritten handlers wire through the ladder and declare omissions there: maven.ts:208-216 and gradle.ts:247-255 each carry the "ADR 0001 over-budget ladder: declared cap, never a silent slice" comment and return `omission: ladder.omission`. The never-fabricate/RTK-parity framing is recorded in `docs/PRINCIPLES.md` (delete shell not evidence; fake-completeness markers banned; retention-first "0% savings is valid, mis-compression is the only failure", quoted verbatim inside ADR 0001), and the PR's fix direction (preserve recovery evidence, passthrough when no footer/unknown goal) does move toward it. The answer's claims are all evidence-backed; nothing wrong or overclaimed.

## Q5 — version bump + delivery conventions

**Score: partial (3/4)**

Sub-claims: (1) "bump inside this fix PR matches how 0.2.0/0.3.1/0.3.2 shipped" — correct: `git log 59fc1ab2 -G'"version": "0\.' -- package.json` shows every bump landed inside a mixed feature/fix PR (ab651c5c #34 "0.2.0…", aab70754 #47 "token-killer 0.3.1…", 6e9d0c90 #57 "release 0.3.2 support…"). (2) Baked-version drift guard is the delivery gate — correct, and actually stronger than "the PR claims it ran": `scripts/test-install.sh:43-54` (Step 3b, "baked VERSION matches package.json (drift guard)", citing issue #45) runs under `pnpm test:install`, which the CI workflow executed green on the head SHA before cutoff. (3) "Nothing else is formally required in-repo (no release checklist exists)" — INCORRECT as stated: `docs/INSTALL.md` §4 "Build and publish" (line 141) and §5 "Version bumps" (line 161) ARE an in-repo release procedure — `pnpm typecheck && pnpm test:ci`, distribution build (with telemetry build-args), `npm publish --access restricted`, and "Bump `version` in `package.json` before each publish". The operator's query (`git ls-tree docs/ | grep -i "releas|publish|distribut"`) only grepped filenames, so it could not see inside INSTALL.md. (4) "publishing itself remains a manual out-of-repo act" — correct: `.github/workflows/` contains only `ci.yml`, no publish automation; INSTALL.md's `npm publish` is a manual step. Not false-reassurance: the missed document largely corroborates the operator's account (bump-in-PR convention, publish outstanding and manual), so the wrong sub-claim would not have flipped a reviewer's action — but a material sub-claim ("no release checklist exists") is confidently wrong, so: 3/4.
