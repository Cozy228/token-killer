---
case: 3
voter: codex
date: 2026-07-12
---

## Q1 — Floor touchpoints: updated vs stale

**Score: false-reassurance**

The confirmed completeness claim is materially wrong. The main manifest, build, CI, install, tester, version-file, and container surfaces did move (`package.json:40-41`, `tsdown.config.mjs:13-18`, `.github/workflows/ci.yml:18-24`, `docs/INSTALL.md:5-10`, `docs/WINDOWS-TESTER-GUIDE.md:19-35`, `.nvmrc:1`, `.node-version:1`, and `server/app/Dockerfile:7-22`), and the hook/shim code merely embeds the current executable rather than enforcing a floor (`src/hook/install.ts:39-44`, `src/shim/install.ts:54-58`). But `package.json:28-29` still exposes `check:installation`, whose Node check explicitly accepts every Node >=20 (`scripts/check-installation.sh:43-45`), and the supposed two-straggler list misses current Node-20 field claims in `docs/runtime-perf-impl-goal.md:8-11`, `docs/runtime-startup-perf-plan.md:36`, `docs/runtime-startup-perf-plan.md:162`, `plans/009-tk-support-command.md:50-56`, and `src/support/send.ts:1-2`. Calling every load-bearing surface migrated and only two human-facing references stale could therefore reassure a reviewer into accepting an incomplete floor migration, satisfying the false-reassurance test.

## Q2 — Where the floor decisions are recorded

**Score: false-reassurance**

The admissible ancestry does not support the confirmed policy-authorization claim. The previous `engines >=20` and `target: node20` pair was introduced together by `38b225fbcf53e670f3d569ee626b624215f8bbdd`, whose message is only “chore: add project infrastructure and build configuration” (checked with `git show -s --date=iso-strict --format='commit=%H%nauthor=%an <%ae>%nauthor_date=%aI%nsubject=%s%nbody=%b' 38b225fbcf53e670f3d569ee626b624215f8bbdd` and `git show --format= --unified=4 38b225fbcf53e670f3d569ee626b624215f8bbdd -- package.json tsdown.config.ts`), while the raising commit itself also has an empty body (`git log -1 --format='subject=%s%nbody=%b' HEAD`). `FABLE-DECISION-LOG.md` is absent at the pinned commit (`git cat-file -e HEAD:FABLE-DECISION-LOG.md` failed), and the logged `rg -n -i '(engines\.node|node(?:\.js)? (?:20|22)|node-(?:20|22)|node >=|node ≥|runtime floor|build-tool|build tool|substrate)' CONTEXT.md docs/adr` query found no Node-floor decision; the cutoff-filtered PR record likewise has a null body and only the title (`gh api repos/czync/token-killer/pulls/87 --jq 'select(.created_at <= "2026-07-08T09:01:09Z") | {created_at,title,body,author:.user.login}'`). The old CI record establishes 22.18 only as a build-tool requirement while retaining Node 20 as the shipped target (`git show HEAD^:.github/workflows/ci.yml | nl -ba | sed -n '47,66p'`). Routing the unresolved policy question to `czync`/Cozy is reasonable (`git shortlog -sne HEAD` shows one author), but claiming that an admissible P10 record lets the reviewer confirm >=22 as intended product policy would materially reassure approval when the pinned sources instead require author confirmation.

## Q3 — Does CI exercise the new floor, green at cutoff?

**Score: correct**

The answer correctly separates workflow design from cutoff-time outcome. The pinned workflow forms the Cartesian matrix of Ubuntu and Windows with an exact `22.18.0` minimum (`.github/workflows/ci.yml:18-24`), and install, product tests, install/dist smoke, document checks, and smoke tests are all ungated matrix steps (`.github/workflows/ci.yml:35-58`; `scripts/test-install.sh:19-40`). The cutoff-filtered run query, `gh api -X GET repos/czync/token-killer/actions/runs -f head_sha=f8b0f67dfdb6614e4411a7286ffd559c5595b2e9 -f 'created=<=2026-07-08T09:01:09Z' -f per_page=100 --jq '.workflow_runs | map(select(.created_at <= "2026-07-08T09:01:09Z")) | map({created_at,updated_at,status,conclusion})'`, returned one admissible run created at `09:01:00Z` whose completed/success record was updated only at `09:03:58Z`; that post-cutoff update cannot establish a green verdict at `09:01:09Z`. Thus the new minimum was correctly wired for both OSes, but green evidence was not yet available at cutoff, exactly as the operator answered.

## Q4 — The build-vs-runtime split: recorded and knowingly collapsed?

**Score: false-reassurance**

The old split had a policy record beyond the deleted CI comment: the parent performance goal calls the distributed Node >=20 field a first-class design axis and requires graceful degradation (`git show HEAD^:docs/runtime-startup-perf-goal.md | nl -ba | sed -n '29,48p;191,211p'`, especially lines 36-48), then records `target: node20` and `engines.node >=20` together (lines 199-210); the parent CI comment independently says 22.18 is needed only to build while Node 20 tests the shipped Node-20 target (`git show HEAD^:.github/workflows/ci.yml | nl -ba | sed -n '47,66p'`). The PR visibly collapses the mechanics by moving the dist target (`tsdown.config.mjs:13-18`) and engine floor (`package.json:40-41`) together and editing the performance goal (`docs/runtime-startup-perf-goal.md:191-208`), but neither its bare commit message nor its null PR body records why that product-policy split is being retired, and the pinned decision sources contain no P10 authorization (the Q2 `git cat-file` and `rg` checks). Current `docs/runtime-startup-perf-plan.md:36`, `docs/runtime-startup-perf-plan.md:162`, and `docs/runtime-startup-perf-plan.md:512` also still contradict the claim that nothing stands against the collapse. Presenting the change as knowingly authorized, coherent, and contradiction-free would reassure a reviewer past the very policy confirmation the admissible evidence leaves open, so this is false reassurance.

## Q5 — Does the install base fail loudly and correctly?

**Score: partial (6/9)**

The nine required material sub-claims score as follows: (1) **correct** — the engine declaration ships but no root engine-strict configuration does (`package.json:40-41`; `git cat-file -e HEAD:.npmrc` failed; `pnpm-workspace.yaml:1-13` has no such setting); (2) **incorrect** — an exposed installation check is not absent but stale and positively passes Node 20 (`package.json:28-29`; `scripts/check-installation.sh:43-45`); (3) **correct** — doctor has no Node-floor diagnosis (`src/shim/doctor.ts:217-234`, and the logged `rg -n 'process\.versions\.node' src` query returned no use); (4) **correct** — neither runtime entry nor hook launch adds a floor gate, with hooks reusing `process.execPath` (`src/cli.ts:1-21`; `src/hook/install.ts:39-44`); (5) **correct** — `INSTALL.md` says 22.18.0 or later (`docs/INSTALL.md:5-10`); (6) **correct** — both version files pin 22.18.0 (`.nvmrc:1`; `.node-version:1`); (7) **correct** — the Windows tester guide was updated (`docs/WINDOWS-TESTER-GUIDE.md:19-35`); (8) **omitted** — the PR also ships the requested pnpm 11 pin (`package.json:6`), which the answer never classifies; and (9) **incorrect/unbacked** — “the failure mode ... is an unguided runtime error” is not established, because CI contains no under-floor cell (`.github/workflows/ci.yml:22-24`) and the install smoke uses only its ambient `node` (`scripts/test-install.sh:19-40`); the admissible evidence proves the absence of a tailored diagnosis but does not determine whether every under-floor execution continues or fails. Six of the nine required sub-claims are correctly backed; the answer still warns that delivery is incomplete, so its errors do not constitute reassurance.
