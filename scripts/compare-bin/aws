#!/bin/sh
# Feed fixture JSON to rtk when it proxies cloudformation describe-stacks.
if [ "$1" = "cloudformation" ] && [ "$2" = "describe-stacks" ]; then
  cat
  exit 0
fi
exec /opt/homebrew/bin/aws "$@"
