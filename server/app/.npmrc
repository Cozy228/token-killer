# Flat node_modules so the pruned prod tree can be COPYed out of the Docker
# builder stage into the Lambda runtime image (pnpm's default symlink layout
# does not survive a cross-stage copy).
node-linker=hoisted
